{
  "name": "keminter-web",
  "version": "1.0.0",
  "private": true,
  "description": "Panel kontrol saklar MQTT lewat WebSocket, statis + satu serverless function",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
